# ビルド指示書：人体アタリ・レイアウトアプリ（仮称：Atari Mannequin）

## 概要

イラスト制作の「アタリ（下描きの当たり）」を素早く作るための、Webベースの**2Dフィギュア・レイアウトエディタ**を作ってください。

日本のイラスト界隈で流行した「**投げ縄ツールで人体の塊（マッス）をざっくり配置してアタリを取る**」やり方を、毎回投げ縄で描く代わりに、**あらかじめ用意した半透明のボディパーツ（頭・胴・腰・上腕・前腕・手・太もも・脛・足…）をドラッグ＆ドロップ＋回転／拡大／反転で参照画像の上に乗せていく**ことで実現します。

最終的に**PNG画像として書き出し**、Clip Studio Paint 等に読み込んでアタリ（下敷き）として使うのが目的です。

## 重要な設計方針（ここを外さないこと）

- **アプリ側で「どこが頭でどこが脚か」を解析しない。** ユーザーが手で各パーツを配置する。画像認識・姿勢推定は一切しない。
- **パーツは完全に独立して動かせること。** 関節で固定して連動させる（IK・親子化）のは「自由度が減る」ので**しない**。各パーツは個別に移動・回転・拡大・反転できる、バラバラのフィギュア部品として扱う。
- 見た目は市販のデッサン人形／可動フィギュアのような**マッス（塊）表現**（添付参考画像のイメージ：丸い関節＋なめらかなテーパーの付いた四肢）。**無彩色グレーの半透明**で、参照画像が透けて見えること。
- あくまで「アタリ（当たり）」用なので、精密な解剖学的正確さより**素早く塊を置ける手触り**を優先。

## 技術スタック（推奨）

- **React 18 + TypeScript + Vite**
- **react-konva（Konva.js）** … キャンバス上のドラッグ／回転・拡大ハンドル（Transformer）／重なり順／`stage.toDataURL()` でのPNG書き出しが標準で揃うため、これを強く推奨。
- 状態管理はReactのuseStateで十分（小規模）。
- 画像・レイアウトの永続化は不要（出力できればよい）。必要になったら IndexedDB を後付け。

## 画面構成

単一のエディタ画面でよい。

```
┌───────────────────────────────────────────────┐
│ ヘッダー：アプリ名 / [画像読込] [PNG書き出し] [全消去] │
├──────────┬────────────────────────────────────┤
│ パーツ    │                                    │
│ トレイ    │        キャンバス（アートボード）      │
│ （一覧）  │     ・参照画像（下敷き）             │
│          │     ・配置されたパーツ群             │
│ 頭        │     ・選択中パーツに変形ハンドル       │
│ 首        │                                    │
│ 胴        │                                    │
│ 腰        │                                    │
│ 上腕 …    │                                    │
├──────────┴────────────────────────────────────┤
│ 選択中ツールバー：反転H / 反転V / 複製 / 前面 / 背面 / 削除 │
│ 下敷き：不透明度スライダー / 表示・非表示トグル              │
│ パーツ全体：不透明度スライダー                            │
└───────────────────────────────────────────────┘
```

## 機能要件（Phase 1 / これがMVP）

### キャンバス（アートボード）
- 固定の論理サイズ（例：900×1200 = 3:4）のアートボードを用意し、画面サイズに合わせて縮小表示（fit）。Konvaなら Layer を `scale` して論理座標のまま扱う。
- 背景は**透明**（白い枠＝紙のように見せるのはCSS側で。書き出しは透過PNGにしたいので、キャンバス自体に不透明な背景矩形を置かない）。

### 参照画像（下敷き）
- **任意の画像ファイルをアップロード**して下敷きにできる（JPG/PNG）。
- アートボードに contain でフィット表示。
- **不透明度スライダー**で薄さを調整できる。
- **表示／非表示トグル**で切り替えられる（非表示にして書き出せば、パーツだけの透過PNGになる）。
- ※下敷きは選択・変形の対象にしない（固定）。

### ボディパーツ
- 左のトレイに**パーツ一覧**を並べ、クリックで（またはドラッグで）キャンバス中央に追加。
- 用意するパーツ（正面想定・各1種。左右はユーザーが反転で作る）：
  - 頭 / 首 / 胴（胸郭）/ 腰（骨盤）/ 上腕 / 前腕 / 手 / 太もも / 脛（下腿）/ 足
  - 余裕があれば 3/4・側面バリエーション、首なしの「胴＋腰一体」なども。
  - さらに余裕があれば「組み立て済みの全身プリセット」を1つ置けると、そこから崩して使えて便利。
- パーツの描画：**無彩色グレーの塗り＋やや濃いグレーの輪郭線**。四肢は**端に関節の丸**を持つ、片側が太く反対が細い**テーパー付きカプセル**で。頭は卵型、胴・腰は角の丸い台形、手・足はしずく／くさび形。
- **半透明**（既定で 50〜60% 程度）。下敷きが透けること。

### パーツ操作
- 選択 → **ドラッグで移動**、**四隅ハンドルで拡大・縮小**、**回転ハンドルで回転**（Konva Transformer）。
- **左右反転 / 上下反転**（ボタン or キー）。`scaleX *= -1` / `scaleY *= -1` で実装。
- **複製 / 削除 / 前面へ / 背面へ（重なり順）**。
- 変形・回転は**パーツ中心を基点**に（offset を形状の中心に設定）。
- 何もない所をクリックで選択解除。
- **タッチ対応**（スマホ／iPadでスワイプ・ピンチ的操作。Konvaが対応）。

### 不透明度
- **パーツ全体の不透明度スライダー**（各パーツの opacity をまとめて制御）。
- 下敷きの不透明度は別スライダー。

### 書き出し
- **PNG書き出し**ボタン。`stage.toDataURL({ pixelRatio })` で**高解像度・背景透過**のPNGを生成しダウンロード。
- 書き出し時は選択ハンドル（Transformer）を一旦外してから出力すること。
- 下敷きを非表示にしていれば、その状態がそのまま出る（＝パーツのみの透過PNG）。これを Clip Studio に「アタリ用レイヤー」として読み込む想定。

## やらないこと（Phase 1 の非対象 / 明確に除外）

- 自動の姿勢推定・人体解析（しない）。
- 関節の固定連動・IK・親子変形（しない＝バラバラのまま）。
- アカウント／クラウド同期／レイアウト保存（不要。出力できればよい）。

## 将来拡張（任意・Phase 2 以降のメモ）

- レイアウトの保存／再開（IndexedDB）。
- 左右ミラー複製（対の手足を一発生成）。
- 関節位置への**スナップ**（くっつけたい時だけ吸着、固定はしない）。
- 投げ縄で**自作ブロブを描いてスタンプ**する機能。
- パーツの濃さ／線の太さ調整、グリッド表示。

## 受け入れ基準（Phase 1 完成の目安）

1. 任意画像を下敷きに読み込め、不透明度と表示/非表示を切り替えられる。
2. トレイから各ボディパーツをキャンバスに追加できる。
3. 追加したパーツを個別に移動・回転・拡大・反転・複製・削除・重なり替えできる。
4. パーツは半透明グレーで、下敷きが透けて見える。
5. 透過PNGとして書き出せ、Clip Studio 等で下敷きとして使える。
6. スマホ／タブレットのタッチでも操作できる。
